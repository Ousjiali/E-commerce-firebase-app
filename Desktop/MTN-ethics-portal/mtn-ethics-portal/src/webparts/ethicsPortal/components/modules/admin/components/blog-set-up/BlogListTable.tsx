import * as React from "react";

type Props = {
  posts: any[];
  isLoading: boolean;
};

export const BlogListTable: React.FC<Props> = ({ posts, isLoading }) => {
  return <div>BlogListTable</div>;
};
